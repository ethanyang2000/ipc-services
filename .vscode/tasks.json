{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make clean && make service",
        },
        {
            "label": "build_all",
            "type": "shell",
            "command": "make clean && make",
        }
    ]
}
